import LocalizedNotFound from './[locale]/not-found';

export default function NotFound() {
  return (
    <html lang="en">
      <body>
        <LocalizedNotFound />
      </body>
    </html>
  );
}
