# .npmrc
engine-strict=true          # enforce engine versions
auto-install-peers=true     # pnpm: auto-install peer deps
shamefully-hoist=false      # pnpm: don't flatten deps (safer)
strict-peer-dependencies=true  # pnpm: fail on peer dependency conflicts